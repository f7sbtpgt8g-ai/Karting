# The worker's dependencies -- deliberately not the repo-root
# requirements.txt (which also carries pytest and other dev-only deps this
# image doesn't need). Keeping the lists apart is also what makes
# `telemetry/analysis.py`'s "no UI libraries" claim
# (tests/test_analysis_extraction.py::test_analysis_module_imports_no_ui_libraries)
# checkable rather than assumed -- a UI import creeping back into
# `telemetry/` stops this image building, not just a lint warning.
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
psycopg2-binary>=2.9
pyarrow>=14
# No HTTP library: Storage and GoTrue are both reached through urllib from
# the standard library (worker/storage_client.py, telemetry/auth.py). One
# less package in an image that holds the service-role key.
